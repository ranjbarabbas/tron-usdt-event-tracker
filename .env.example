# .env.example
TRONGRID_API_KEY=            # TronGrid API key
CONTRACT_ADDRESS=            # TRC20 contract address (e.g. USDT on Tron)
POLL_INTERVAL_MS=3000        # poll interval in milliseconds
DB_PATH=./data/events.db     # sqlite database path
PORT=3000
MAX_RETRY=5
TRONGRID_BASE=https://api.trongrid.io
PAGE_LIMIT=200               # events per request (TronGrid supports paging)
